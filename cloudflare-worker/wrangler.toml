name            = "paax-stream"
main            = "worker.js"
compatibility_date = "2024-09-23"

# Routes — replace zone_name with your actual zone if using wrangler deploy
# [[routes]]
# pattern = "stream.paaxmusic.app/*"
# zone_name = "paaxmusic.app"

[observability]
enabled = true

[vars]
# No secrets needed for V1 — Innertube is called without auth.
# Add YOUTUBE_API_KEY here if you upgrade to authenticated Innertube later.
